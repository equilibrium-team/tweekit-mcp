# Copy this file to `.env` (ignored by git) and fill in secrets before running
# local development or smoke tests. Values shown here point to the hosted
# streamable HTTP endpoint and can be swapped for staging or local deployments.

TWEEKIT_API_KEY=replace-with-your-api-key
TWEEKIT_API_SECRET=replace-with-your-api-secret

# Streamable HTTP endpoint for the MCP server.
TWEEKIT_MCP_BASE_URL=https://mcp.tweekit.io/mcp/

# Optional: override the upstream TweekIT REST API (defaults to production).
# TWEEKIT_API_BASE_URL=https://dapp.tweekit.io/tweekit/api/image/
